// Set the TypeScript interface of the properties of the returned FileType.

export interface FileType {
    mimetype: string
    originalname: string
    destination: string
    filename: string
    path: string
    size: string
}
